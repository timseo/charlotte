{% assign top = top | default: section.settings.top %}
{% assign bottom = bottom | default: section.settings.bottom %}

{% assign blocks = section.blocks %}

{% assign arrows = arrows | default: section.settings.carousel-arrows %}
{% assign dots = dots | default: section.settings.carousel-dots %}

{% if blocks.size > 0 %}
<div class="product-recommendations__inner halo-section padding-top-{{ top }} padding-bottom-{{ bottom }}">
  <div class="halo-row column-{{ column }} text-center"
       data-slick-pc data-slick='{
                                 "rows": 0,
                                 "rtl": false,
                                 "dots": {{ dots }},
                                 "arrows": {{ arrows }},
                                 "infinite": false,
                                 "adaptiveHeight": true,
                                 "slidesToShow": 5,
                                 "slidesToScroll": 1,
                                 "prevArrow": "<div class=&apos;slick-prev slick-arrow slick-arrow--large&apos;><svg class=&apos;icon&apos;><use xlink:href=&apos;#icon-chevron-left&apos; /></svg></div>", 
                                 "nextArrow": "<div class=&apos;slick-next slick-arrow slick-arrow--large&apos;><svg class=&apos;icon&apos;><use xlink:href=&apos;#icon-chevron-right&apos; /></svg></div>"
                                 }'>
    
    {% for block in blocks %}
    	{% case block.type %}
    		{% when 'image' %}
              {% assign img_url = block.settings.image | image_url %}
              {% assign title = block.settings.title %}
              {% assign description = block.settings.description %}
    
              <div class="halo-row__item">
                  {% render 'product-card-grid-slider' with img_url, img_url: img_url, title: title, description: description %}
              </div>
    	{% endcase %}
    {% endfor %}
    	
  </div>
</div>
{% endif %}


{% schema %}
{
    "name": "Product Slider",
    "settings": [
        {
            "type": "header",
            "content": "Section Layout"
        },
        {
            "type": "range",
            "id": "top",
            "label": "Spacing Top",
            "default": 60,
            "min": 0,
            "max": 100,
            "step": 10,
            "unit": "px"
        },
        {
            "type": "range",
            "id": "bottom",
            "label": "Spacing bottom",
            "default": 60,
            "min": 0,
            "max": 100,
            "step": 10,
            "unit": "px"
        },
        {
            "type": "header",
            "content": "Layout"
        },
        {
            "type": "checkbox",
            "id": "carousel-arrows",
            "label": "show arrows (Layout carousel)",
            "default": false
        },
        {
            "type": "checkbox",
            "id": "carousel-dots",
            "label": "show dots (Layout carousel)",
            "default": false
        }
    ],
    "blocks": [
       {
         "name": "Image",
         "type": "image",
         "settings": [
            {
              "type": "header",
              "content": "Image Slider"
            },
           {
             "type": "image_picker",
             "id": "image",
             "label": "Image"
           },
			{
              "type" : "text",
              "id" : "title",
              "label" : "Title"
        	},
			{
            "type" : "textarea",
            "id" : "description",
            "label" : "Write a Description..."
        	}
         ]
       }
     ]
}
{% endschema %}

{% stylesheet %}
{% endstylesheet %}

{% javascript %}
{% endjavascript %}
{% comment %}

This file has been rewritten to preserve the original behavior.

---
Changes:
- from: "{% render 'product-card-grid-slider' with img_url: img_url, title: title,
    description: description %}"
  to: "{% render 'product-card-grid-slider' with img_url, img_url: img_url, title:
    title, description: description %}"
  position: 1732..1842
{% endcomment %}