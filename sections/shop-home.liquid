{% if section.settings.default_layout %}
  {% if section.settings.hide_breadcrumb %}
    {% if settings.breadcrumb_layout == 'top' %}
        {% render 'breadcrumb', breadcrumb_top: true %}
    {% endif %}
  {% endif %}
  <div class="container">
      <div class="page page-default">
          <div class="section-header text-center">
              <h1 class="title title--large">{{ page_title }}</h1>
          </div>
          <div class="rte">
              {{ page.content }}
          </div>
      </div>
  </div>
  {% if section.settings.hide_breadcrumb %}
    {% if settings.breadcrumb_layout == 'bottom' %}
        {% render 'breadcrumb' %}
    {% endif %}
  {% endif %}

{% else %}

{%- liquid
  assign margin = section.settings.margin | split: ','
  assign margin_top = margin[0]
  assign margin_bottom = margin[1]
  assign padding = section.settings.padding | split: ','
  assign padding_top = padding[0]
  assign padding_bottom = padding[1]
  %}

<div class="{% render 'container-settings' %}" data-section-id="{{ section.id }}" data-section-type="collection-template">          
  <div class="row"> 
    <div class="dt-sc-column">
      {{ page.content }}
    </div>
  </div>
</div>

<style>
  @media only screen and (min-width: 1200px) { #shopify-section-{{ section.id }}.main-page-section .row > div { margin-top:{{ margin_top }}px; margin-bottom:{{ margin_top }}px;padding-top:{{ padding_top }}px; padding-bottom:{{ padding_bottom }}px; } }
  @media only screen and (max-width: 1199px) { #shopify-section-{{ section.id }}.main-page-section .row > div { margin-top:{{ margin_top | divided_by: 2 }}px; margin-bottom:{{ margin_bottom | divided_by: 2 }}px;padding-top:{{ padding_top | divided_by: 2 }}px; padding-bottom:{{ padding_bottom | divided_by: 2 }}px;} }
</style>
{% endif %}
{% schema %}
{
  "name": "Page",
  "tag": "section",
  "class": "main-page-section",
  "settings": [
    {
      "type": "checkbox",
      "id": "full",
      "label": "Show Full width"
    },
    {
      "type": "checkbox",
      "id": "spacing_both_ends",
      "label": "Enable Right & Left Spacing (Works only on Fullwidth)",
      "default": false
    },
    {
      "type": "text",
      "id": "padding",
      "label": "Style value (Padding)",
      "default": "0,0",
      "info": "Top(px),Bottom(px)"
    },
    {
      "type": "text",
      "id": "margin",
      "label": "Style value (Margin)",
      "default": "0,0",
      "info": "Top(px),Bottom(px)"
    },
    {
      "type": "checkbox",
      "id": "default_layout",
      "label": "Default layout"
    },
    {
      "type": "checkbox",
      "id": "hide_breadcrumb",
      "label": "Hide breadcrumb"
    }
  ]
}
{% endschema %}