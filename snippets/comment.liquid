<!-- /snippets/comment.liquid -->
<div class="comment__icon">
    <svg class="icon"><use xlink:href="#icon-user" /></svg>
</div>
<div class="comment__meta">
    <span class="comment__meta-item" {% if settings.enable_multilang %}data-translate="blogs.article.posted_in"{% endif %}>{{ 'blogs.article.posted_in' | t }}</span>
    <span class="comment__meta-item">{{ comment.created_at | time_tag: format: 'date' }}</span>
    <span class="comment__meta-item">{{ comment.author }}</span>
</div>
<div class="comment__content rte">
    {{ comment.content }}
</div>
