
<div
  class="selector-wrapper selector-wrapper-{{forloop.index}} swatch js product-form__item"
  data-option-index="{{forloop.index}}"
>
  <label class="form-label {% if option.name == 'default' %}label--hidden{% endif %}" style="display: none;">
    {{ option.name }}: <span class="label-value-{{ forloop.index }}">{{ option.selected_value }}</span>
  </label>
  {% assign option_index0 = forloop.index0 %}
  {% assign option_index = forloop.index %}
  {% assign values = '' %}

  <div class="swatch-element--container">
    {% for variant in product.variants %}
      {% assign value = variant.options[option_index0] %}
      {% unless values contains value %}
        {% assign values = values | join: ';' %}
        {% assign values = values | append: ';' | append: value %}
        {% assign values = values | split: ';' %}
        {% assign value_2 = value | remove: ' ' | downcase %}
        <div
          class="swatch-element size {{ value_2 }}{% if variant.available %} available{% else %} soldout{% endif %}"
          data-value="{{ value }}"
        >
          <input
            {% unless variant.available %}
              disabled
            {% endunless %}
            class="single-option-selector single-option-selector-{{ section.id }}"
            type="radio"
            name="SingleOptionSelector-{{ option_index0 }}"
            data-index="option{{ option_index }}"
            value="{{ value | escape }}"
            {% if option.selected_value == value %}
              checked="checked"
            {% endif %}
            id="SingleOptionSelector{{ option_index0 }}-{{ value }}"
          >
          <label
            data-toggle="tooltip"
            data-placement="top"
            title="{{ value }}"
            for="SingleOptionSelector{{ option_index0 }}-{{ value }}"
          >
            <p>{{- value -}}</p>
            <p class="description">{{ variant.metafields.custom.variant_size_description }}</p>
            <p class="price">{{ variant.price | money }}</p>
          </label>
        </div>
      {% endunless %}
    {% endfor %}
  </div>
</div>
